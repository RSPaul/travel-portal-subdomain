$(document).ready(function() {
  
  if(document.querySelector( '#editor1' )) {
  ClassicEditor
    .create( document.querySelector( '#editor1' ), {
      // toolbar: [ 'heading', '|', 'bold', 'italic', 'link' ]
    } )
    .then( editor => {
      window.editor = editor;
    } )
    .catch( err => {
      console.error( err.stack );
    } );

  }

  if(document.querySelector( '#editor2' )) {
    ClassicEditor
    .create( document.querySelector( '#editor2' ), {
      // toolbar: [ 'heading', '|', 'bold', 'italic', 'link' ]
    } )
    .then( editor => {
      window.editor = editor;
    } )
    .catch( err => {
      console.error( err.stack );
    } );
  }

  if(document.querySelector( '#editor3' )) {
    ClassicEditor
    .create( document.querySelector( '#editor3' ), {
      // toolbar: [ 'heading', '|', 'bold', 'italic', 'link' ]
    } )
    .then( editor => {
      window.editor = editor;
    } )
    .catch( err => {
      console.error( err.stack );
    } );
  }

  if(document.querySelector( '#editor7' )) {
    ClassicEditor
    .create( document.querySelector( '#editor7' ), {
      // toolbar: [ 'heading', '|', 'bold', 'italic', 'link' ]
    } )
    .then( editor => {
      window.editor = editor;
    } )
    .catch( err => {
      console.error( err.stack );
    } );
  }

  if(document.querySelector( '#editor4' )) {
    ClassicEditor
    .create( document.querySelector( '#editor4' ), {
      // toolbar: [ 'heading', '|', 'bold', 'italic', 'link' ]
    } )
    .then( editor => {
      window.editor = editor;
    } )
    .catch( err => {
      console.error( err.stack );
    } );
  }

  if(document.querySelector( '#editor5' )) {
    ClassicEditor
    .create( document.querySelector( '#editor5' ), {
      // toolbar: [ 'heading', '|', 'bold', 'italic', 'link' ]
    } )
    .then( editor => {
      window.editor = editor;
    } )
    .catch( err => {
      console.error( err.stack );
    } );
  }

  if(document.querySelector( '#editor8' )) {
    ClassicEditor
    .create( document.querySelector( '#editor8' ), {
      // toolbar: [ 'heading', '|', 'bold', 'italic', 'link' ]
    } )
    .then( editor => {
      window.editor = editor;
    } )
    .catch( err => {
      console.error( err.stack );
    } );
  }

  if(document.querySelector( '#editor9' )) {
    ClassicEditor
    .create( document.querySelector( '#editor9' ), {
      // toolbar: [ 'heading', '|', 'bold', 'italic', 'link' ]
    } )
    .then( editor => {
      window.editor = editor;
    } )
    .catch( err => {
      console.error( err.stack );
    } );
  }

  if(document.querySelector( '#editor10' )) {
    ClassicEditor
    .create( document.querySelector( '#editor10' ), {
      // toolbar: [ 'heading', '|', 'bold', 'italic', 'link' ]
    } )
    .then( editor => {
      window.editor = editor;
    } )
    .catch( err => {
      console.error( err.stack );
    } );
  }

  if(document.querySelector( '#editor11' )) {
    ClassicEditor
    .create( document.querySelector( '#editor11' ), {
      // toolbar: [ 'heading', '|', 'bold', 'italic', 'link' ]
    } )
    .then( editor => {
      window.editor = editor;
    } )
    .catch( err => {
      console.error( err.stack );
    } );
  }

  if(document.querySelector( '#editor12' )) {
    ClassicEditor
    .create( document.querySelector( '#editor12' ), {
      // toolbar: [ 'heading', '|', 'bold', 'italic', 'link' ]
    } )
    .then( editor => {
      window.editor = editor;
    } )
    .catch( err => {
      console.error( err.stack );
    } );
  }

  if(document.querySelector( '#editor13' )) {
    ClassicEditor
    .create( document.querySelector( '#editor13' ), {
      // toolbar: [ 'heading', '|', 'bold', 'italic', 'link' ]
    } )
    .then( editor => {
      window.editor = editor;
    } )
    .catch( err => {
      console.error( err.stack );
    } );
  }

  if(document.querySelector( '#editor14' )) {
    ClassicEditor
    .create( document.querySelector( '#editor14' ), {
      // toolbar: [ 'heading', '|', 'bold', 'italic', 'link' ]
    } )
    .then( editor => {
      window.editor = editor;
    } )
    .catch( err => {
      console.error( err.stack );
    } );
  }
  });